<Window x:Class="DCCExpressHub.Desktop.MainWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
 Title="DCCExpressHub" Width="1400" Height="900" MinWidth="900" MinHeight="600"
 WindowStartupLocation="CenterScreen" PreviewKeyDown="Window_PreviewKeyDown">
 <DockPanel>
   <Menu x:Name="MainMenu" DockPanel.Dock="Top" Visibility="Collapsed">
     <MenuItem Header="_View">
       <MenuItem x:Name="ServerLogMenuItem"
                 Header="Server _Log"
                 IsCheckable="True"
                 Click="ServerLogMenuItem_Click"/>
     </MenuItem>
   </Menu>

   <Grid>
     <Grid.RowDefinitions>
       <RowDefinition Height="*"/>
       <RowDefinition x:Name="LogRow" Height="0"/>
     </Grid.RowDefinitions>

     <wv2:WebView2 x:Name="Browser" Grid.Row="0"/>

     <Border Grid.Row="1" BorderThickness="0,1,0,0" BorderBrush="#444" Background="#111827">
       <DockPanel>
         <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="8,5">
           <TextBlock Text="DCCExpressHub server log" Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center"/>
           <Button Content="Clear" Margin="12,0,0,0" Padding="10,2" Click="ClearLog_Click"/>
         </StackPanel>
         <TextBox x:Name="ServerLogText"
                  IsReadOnly="True"
                  AcceptsReturn="True"
                  TextWrapping="NoWrap"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Auto"
                  FontFamily="Consolas"
                  FontSize="12"
                  Background="#0B1220"
                  Foreground="#D1D5DB"
                  BorderThickness="0"/>
       </DockPanel>
     </Border>

     <Border x:Name="StartupOverlay" Grid.RowSpan="2" Background="#EE111827">
       <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
         <TextBlock Text="DCCExpressHub" Foreground="White" FontSize="32" FontWeight="SemiBold" HorizontalAlignment="Center"/>
         <TextBlock x:Name="StartupText" Text="Backend indítása…" Foreground="#FFD1D5DB" FontSize="16" Margin="0,14,0,0" HorizontalAlignment="Center"/>
       </StackPanel>
     </Border>
   </Grid>
 </DockPanel>
</Window>
