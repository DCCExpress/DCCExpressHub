<Window x:Class="DCCExpressHub.Desktop.StartupSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DCCExpressHub settings"
        Width="780"
        Height="800"
        MinWidth="720"
        MinHeight="680"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#182230"/>
        <SolidColorBrush x:Key="PanelBorderBrush" Color="#2D3B4E"/>
        <SolidColorBrush x:Key="TextBrush" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#94A3B8"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#38BDF8"/>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="Foreground" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!--
            Do not rely on the Windows default Button / ComboBox templates here.
            Their theme-specific hover/dropdown colors can turn nearly white,
            making the dark launcher unreadable.
        -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#243247"/>
            <Setter Property="Foreground" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#3B4C63"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#334155"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#94A3B8"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#1E293B"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#38BDF8"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#38BDF8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#172033"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#263244"/>
                                <Setter Property="Foreground" Value="#64748B"/>
                                <Setter Property="Opacity" Value="0.75"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button"
               BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#0369A1"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#38BDF8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20,9"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="Foreground" Value="#F8FAFC"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#1E3A5F"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#075985"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#64748B"/>
                                <Setter Property="Opacity" Value="0.65"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="Foreground" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="MinHeight" Value="38"/>
            <Setter Property="MaxDropDownHeight" Value="300"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="ComboBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6"
                                    SnapsToDevicePixels="True"/>

                            <Grid Margin="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="34"/>
                                </Grid.ColumnDefinitions>

                                <ContentPresenter x:Name="ContentSite"
                                                  Grid.Column="0"
                                                  Margin="9,4,4,4"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="Center"
                                                  IsHitTestVisible="False"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"/>

                                <TextBox x:Name="PART_EditableTextBox"
                                         Grid.Column="0"
                                         Margin="7,2,3,2"
                                         Padding="2,0"
                                         HorizontalContentAlignment="Left"
                                         VerticalContentAlignment="Center"
                                         Background="Transparent"
                                         Foreground="{TemplateBinding Foreground}"
                                         BorderThickness="0"
                                         CaretBrush="#F8FAFC"
                                         SelectionBrush="#0369A1"
                                         IsReadOnly="{TemplateBinding IsReadOnly}"
                                         Visibility="Hidden"/>

                                <ToggleButton x:Name="DropDownToggle"
                                              Grid.Column="1"
                                              Focusable="False"
                                              ClickMode="Press"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              Cursor="Hand">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border x:Name="ArrowBackground"
                                                    Background="Transparent"
                                                    CornerRadius="0,5,5,0">
                                                <Path x:Name="Arrow"
                                                      Width="10"
                                                      Height="6"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      Fill="#CBD5E1"
                                                      Data="M 0 0 L 10 0 L 5 6 Z"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="ArrowBackground" Property="Background" Value="#26364A"/>
                                                    <Setter TargetName="Arrow" Property="Fill" Value="#FFFFFF"/>
                                                </Trigger>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter TargetName="ArrowBackground" Property="Background" Value="#1E3A5F"/>
                                                    <Setter TargetName="Arrow" Property="Fill" Value="#7DD3FC"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                            </Grid>

                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Background="#0B1220"
                                        BorderBrush="#475569"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Margin="0,3,0,0"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ScrollViewer Margin="2"
                                                  SnapsToDevicePixels="True"
                                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ComboBorder" Property="Background" Value="#162338"/>
                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="#64748B"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="#38BDF8"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="ComboBorder" Property="Background" Value="#111F33"/>
                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="#38BDF8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ComboBorder" Property="Background" Value="#172033"/>
                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="#263244"/>
                                <Setter Property="Foreground" Value="#64748B"/>
                                <Setter Property="Opacity" Value="0.75"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark compact scrollbars for the launcher and ComboBox dropdowns. -->
        <Style x:Key="ScrollBarLineButtonStyle" TargetType="RepeatButton">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollBarPageButtonStyle" TargetType="RepeatButton">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollBarThumbStyle" TargetType="Thumb">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="MinWidth" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="ThumbBorder"
                                Margin="2"
                                Background="#64748B"
                                CornerRadius="5"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbBorder"
                                        Property="Background"
                                        Value="#64748B"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbBorder"
                                        Property="Background"
                                        Value="#38BDF8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ThumbBorder"
                                        Property="Background"
                                        Value="#263244"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="#111827"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="Width" Value="12"/>
            <Setter Property="MinWidth" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid x:Name="Root"
                              Background="{TemplateBinding Background}">
                            <Grid x:Name="VerticalRoot">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="0"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="0"/>
                                </Grid.RowDefinitions>

                                <RepeatButton Grid.Row="0"
                                              Command="ScrollBar.LineUpCommand"
                                              Style="{StaticResource ScrollBarLineButtonStyle}"/>

                                <Track x:Name="PART_Track"
                                       Grid.Row="1"
                                       IsDirectionReversed="True"
                                       Focusable="False">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Command="ScrollBar.PageUpCommand"
                                                      Style="{StaticResource ScrollBarPageButtonStyle}"/>
                                    </Track.DecreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb Style="{StaticResource ScrollBarThumbStyle}"/>
                                    </Track.Thumb>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Command="ScrollBar.PageDownCommand"
                                                      Style="{StaticResource ScrollBarPageButtonStyle}"/>
                                    </Track.IncreaseRepeatButton>
                                </Track>

                                <RepeatButton Grid.Row="2"
                                              Command="ScrollBar.LineDownCommand"
                                              Style="{StaticResource ScrollBarLineButtonStyle}"/>
                            </Grid>

                            <Grid x:Name="HorizontalRoot"
                                  Visibility="Collapsed">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="0"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="0"/>
                                </Grid.ColumnDefinitions>

                                <RepeatButton Grid.Column="0"
                                              Command="ScrollBar.LineLeftCommand"
                                              Style="{StaticResource ScrollBarLineButtonStyle}"/>

                                <Track x:Name="PART_HorizontalTrack"
                                       Grid.Column="1"
                                       IsDirectionReversed="False"
                                       Focusable="False">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Command="ScrollBar.PageLeftCommand"
                                                      Style="{StaticResource ScrollBarPageButtonStyle}"/>
                                    </Track.DecreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb Style="{StaticResource ScrollBarThumbStyle}"/>
                                    </Track.Thumb>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Command="ScrollBar.PageRightCommand"
                                                      Style="{StaticResource ScrollBarPageButtonStyle}"/>
                                    </Track.IncreaseRepeatButton>
                                </Track>

                                <RepeatButton Grid.Column="2"
                                              Command="ScrollBar.LineRightCommand"
                                              Style="{StaticResource ScrollBarLineButtonStyle}"/>
                            </Grid>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter Property="Width"
                                        Value="Auto"/>
                                <Setter Property="MinWidth"
                                        Value="0"/>
                                <Setter Property="Height"
                                        Value="12"/>
                                <Setter Property="MinHeight"
                                        Value="12"/>
                                <Setter TargetName="VerticalRoot"
                                        Property="Visibility"
                                        Value="Collapsed"/>
                                <Setter TargetName="HorizontalRoot"
                                        Property="Visibility"
                                        Value="Visible"/>
                            </Trigger>
                            <Trigger Property="Orientation" Value="Vertical">
                                <Setter Property="Width"
                                        Value="12"/>
                                <Setter Property="MinWidth"
                                        Value="12"/>
                                <Setter Property="Height"
                                        Value="Auto"/>
                                <Setter Property="MinHeight"
                                        Value="0"/>
                                <Setter TargetName="VerticalRoot"
                                        Property="Visibility"
                                        Value="Visible"/>
                                <Setter TargetName="HorizontalRoot"
                                        Property="Visibility"
                                        Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="#E2E8F0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,2,20,2"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Border Background="#111827"
            BorderBrush="#334155"
            BorderThickness="1"
            CornerRadius="14">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="#0B1220"
                    CornerRadius="14,14,0,0"
                    Padding="24,18"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="DCCEXPRESSHUB"
                                       Foreground="#F8FAFC"
                                       FontWeight="Bold"
                                       FontSize="25"
                                       VerticalAlignment="Center"/>
                            <Border Background="#17324A"
                                    BorderBrush="#1D4F73"
                                    BorderThickness="1"
                                    CornerRadius="12"
                                    Padding="9,3"
                                    Margin="12,2,0,0">
                                <TextBlock x:Name="VersionText"
                                           Foreground="#7DD3FC"
                                           FontSize="12"
                                           FontWeight="SemiBold"/>
                            </Border>
                        </StackPanel>
                        <TextBlock x:Name="SubtitleText"
                                   Text="Desktop startup settings"
                                   Foreground="#94A3B8"
                                   FontSize="13"
                                   Margin="0,5,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal"
                                VerticalAlignment="Center"
                                Margin="20,0,14,0">
                        <TextBlock x:Name="LanguageLabelText"
                                   Text="Language"
                                   Foreground="#94A3B8"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"/>
                        <ComboBox x:Name="LanguageCombo"
                                  Width="128"
                                  SelectionChanged="LanguageCombo_SelectionChanged">
                            <ComboBoxItem Content="English" Tag="en"/>
                            <ComboBoxItem Content="Magyar" Tag="hu"/>
                            <ComboBoxItem Content="Deutsch" Tag="de"/>
                        </ComboBox>
                    </StackPanel>

                    <Button Grid.Column="2"
                            Content="✕"
                            Width="34"
                            Height="34"
                            Padding="0"
                            FontSize="15"
                            Background="#172033"
                            BorderBrush="#334155"
                            Click="CancelButton_Click"/>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="24,18,24,12">
                <StackPanel>

                    <Border Background="{StaticResource PanelBrush}"
                            BorderBrush="{StaticResource PanelBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="10"
                            Padding="18"
                            Margin="0,0,0,14">
                        <StackPanel>
                            <TextBlock x:Name="ConnectionTitleText" Text="DCC-EX connection"
                                       Foreground="{StaticResource TextBrush}"
                                       FontWeight="SemiBold"
                                       FontSize="16"/>
                            <TextBlock x:Name="ConnectionDescriptionText" Text="Choose how the desktop backend connects to the command station."
                                       Foreground="{StaticResource MutedTextBrush}"
                                       Margin="0,3,0,13"
                                       FontSize="12"/>

                            <TextBlock x:Name="ProtocolLabelText" Text="Protocol"
                                       Foreground="#CBD5E1"
                                       Margin="0,0,0,5"/>
                            <ComboBox x:Name="ProtocolCombo"
                                      SelectionChanged="ProtocolCombo_SelectionChanged">
                                <ComboBoxItem x:Name="ProtocolChooseItem" Content="Choose…" Tag=""/>
                                <ComboBoxItem x:Name="ProtocolTcpItem" Content="DCC-EX TCP" Tag="tcp"/>
                                <ComboBoxItem x:Name="ProtocolSerialItem" Content="DCC-EX Serial" Tag="serial"/>
                            </ComboBox>

                            <Grid x:Name="TcpPanel" Margin="0,12,0,0" Visibility="Collapsed">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="120"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock x:Name="TcpHostLabelText" Text="IP / host"
                                               Foreground="#CBD5E1"
                                               Margin="0,0,0,5"/>
                                    <TextBox x:Name="TcpHostText"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock x:Name="TcpPortLabelText" Text="Port"
                                               Foreground="#CBD5E1"
                                               Margin="0,0,0,5"/>
                                    <TextBox x:Name="TcpPortText"/>
                                </StackPanel>
                            </Grid>

                            <Grid x:Name="SerialPanel" Margin="0,12,0,0" Visibility="Collapsed">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock x:Name="SerialPortLabelText" Text="COM port"
                                               Foreground="#CBD5E1"
                                               Margin="0,0,0,5"/>
                                    <ComboBox x:Name="SerialPortCombo"
                                              IsEditable="True"
                                              IsTextSearchEnabled="True"/>
                                </StackPanel>
                                <Button x:Name="RefreshSerialButton"
                                        Grid.Column="1"
                                        Content="Refresh"
                                        VerticalAlignment="Bottom"
                                        Click="RefreshSerialButton_Click"/>
                            </Grid>

                            <StackPanel Orientation="Horizontal" Margin="0,14,0,0">
                                <Button x:Name="TestButton"
                                        Content="Test connection"
                                        Click="TestButton_Click"/>
                                <Border x:Name="TestStatusBorder"
                                        Visibility="Collapsed"
                                        Background="#10243A"
                                        BorderBrush="#285172"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Padding="10,7"
                                        Margin="10,0,0,0">
                                    <TextBlock x:Name="TestStatusText"
                                               Foreground="#BAE6FD"
                                               VerticalAlignment="Center"/>
                                </Border>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Background="{StaticResource PanelBrush}"
                            BorderBrush="{StaticResource PanelBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="10"
                            Padding="18"
                            Margin="0,0,0,14">
                        <StackPanel>
                            <TextBlock x:Name="WebServerTitleText" Text="Web server"
                                       Foreground="{StaticResource TextBrush}"
                                       FontWeight="SemiBold"
                                       FontSize="16"/>
                            <TextBlock x:Name="WebServerDescriptionText" Text="In Local mode the Hub is available only on this computer. In Server mode it is also available on the LAN."
                                       Foreground="{StaticResource MutedTextBrush}"
                                       Margin="0,3,0,13"
                                       FontSize="12"/>

                            <StackPanel Orientation="Horizontal">
                                <RadioButton x:Name="LocalModeRadio"
                                             GroupName="RunMode"
                                             Content="Local (127.0.0.1)"/>
                                <RadioButton x:Name="ServerModeRadio"
                                             GroupName="RunMode"
                                             Content="Server (0.0.0.0)"/>
                            </StackPanel>

                            <Grid Margin="0,12,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="140"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="HttpPortLabelText"
                                           Grid.Column="0"
                                           Text="HTTP port"
                                           Foreground="#CBD5E1"
                                           VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1"
                                         x:Name="HttpPortText"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Background="{StaticResource PanelBrush}"
                            BorderBrush="{StaticResource PanelBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="10"
                            Padding="18">
                        <StackPanel>
                            <TextBlock x:Name="WorkspaceTitleText" Text="Workspace"
                                       Foreground="{StaticResource TextBrush}"
                                       FontWeight="SemiBold"
                                       FontSize="16"/>
                            <TextBlock x:Name="WorkspaceDescriptionText" Text="This is a persistent location independent of builds. Layouts, locomotives, automations and your own files remain in its data folder."
                                       Foreground="{StaticResource MutedTextBrush}"
                                       TextWrapping="Wrap"
                                       Margin="0,3,0,13"
                                       FontSize="12"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0"
                                         x:Name="WorkspaceText"
                                         Margin="0,0,10,0"/>
                                <Button x:Name="BrowseWorkspaceButton"
                                        Grid.Column="1"
                                        Content="Browse…"
                                        Click="BrowseWorkspaceButton_Click"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock x:Name="ValidationText"
                               Foreground="#FDA4AF"
                               TextWrapping="Wrap"
                               Margin="2,12,2,0"/>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="2"
                    Background="#0B1220"
                    CornerRadius="0,0,14,14"
                    Padding="24,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="FooterHintText"
                               Grid.Column="0"
                               Text="Settings are remembered, but this window is shown on every startup."
                               Foreground="#64748B"
                               FontSize="11"
                               VerticalAlignment="Center"
                               TextWrapping="Wrap"/>

                    <Button x:Name="CancelButton"
                            Grid.Column="1"
                            Content="Cancel"
                            Margin="12,0,8,0"
                            Click="CancelButton_Click"/>

                    <Button x:Name="OkButton"
                            Grid.Column="2"
                            Content="OK · Start backend"
                            Style="{StaticResource PrimaryButton}"
                            Click="OkButton_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
