diff --git a/src/ApiServer.cpp b/src/ApiServer.cpp
--- a/src/ApiServer.cpp
+++ b/src/ApiServer.cpp
@@
 void ApiServer::sendFsFile(
     AsyncWebServerRequest* request,
     const String& requestedPath) {
@@
   const String gz = path + ".gz";
+
+  const auto cacheControlFor =
+      [](const String& filePath) -> const char* {
+    // Runtime/config/static JSON must never be served from browser cache.
+    if (filePath.endsWith(".json")) {
+      return "no-store";
+    }
+
+    // During Hub development we keep fixed app-v2.js / index-v2.css names.
+    // Therefore JS/CSS/HTML must be revalidated every time instead of being
+    // marked immutable for a year.
+    if (
+        filePath.endsWith(".js") ||
+        filePath.endsWith(".css") ||
+        filePath.endsWith(".html") ||
+        filePath.endsWith(".map")) {
+      return "no-cache";
+    }
+
+    // Images may remain cached. One day is enough to reduce ESP32 traffic
+    // without making changed images sticky for months.
+    if (
+        filePath.endsWith(".png") ||
+        filePath.endsWith(".jpg") ||
+        filePath.endsWith(".jpeg") ||
+        filePath.endsWith(".webp") ||
+        filePath.endsWith(".gif") ||
+        filePath.endsWith(".svg") ||
+        filePath.endsWith(".ico")) {
+      return "public, max-age=86400";
+    }
+
+    // Fonts are content-like static assets and rarely change.
+    if (
+        filePath.endsWith(".woff") ||
+        filePath.endsWith(".woff2")) {
+      return "public, max-age=31536000, immutable";
+    }
+
+    return "no-cache";
+  };

   if (LittleFS.exists(gz)) {
@@
     response->addHeader(
         "Cache-Control",
-        path.startsWith("/assets/")
-            ? "public, max-age=31536000, immutable"
-            : "no-cache");
+        cacheControlFor(path));
@@
     response->addHeader(
         "Cache-Control",
-        path.startsWith("/assets/")
-            ? "public, max-age=31536000, immutable"
-            : "no-cache");
+        cacheControlFor(path));
