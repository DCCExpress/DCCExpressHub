CURRENT REPO: a20b1c5cc1f221d30d8a9e7972f0911407bba104

clientWsCommands.ts
-------------------
In ClientWsPayloadMap add:

  getRuntimeDebugSnapshot: EmptyClientWsCommandPayload;


wsTypes.ts
----------
In CLIENT_WS_MESSAGE_TYPES add:

  "getRuntimeDebugSnapshot",

In ServerWsPayloadMap add:

  sensorDebugChanged: {
    address: number;
    on: boolean;
  };

  runtimeDebugSnapshot: {
    sensors: Array<{
      address: number;
      on: boolean;
    }>;
  };

No change is required to the normal sensorChanged / sensorSnapshot contracts.
Those remain layout-runtime events.
